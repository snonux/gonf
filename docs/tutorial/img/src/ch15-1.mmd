flowchart LR
    D["`**1. declare**
        main runs
        *declaration error*`"] --> R["`**2. record**
        task bodies run
        *record error*`"] --> P["`**3. push checks**
        before any ssh
        *push refusal*`"] --> A["`**4. apply**
        on the destination
        *apply error*`"]
